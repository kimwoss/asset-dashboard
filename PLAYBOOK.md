# 분당부부 시스템 플레이북

> 어느 노트북에서 이어받든 이 문서 하나로 전체 그림을 잡을 수 있게 쓴다.
> 새 사고를 겪으면 **§5 사고 기록**에 반드시 한 줄 남긴다 — 그게 이 문서의 값이다.
>
> 비밀번호·토큰·계좌번호는 절대 여기 적지 않는다. 공개 레포다.

---

## 1. 한 장 지도

```
                 ┌─────────────────────────────────────────┐
   사람 ────────▶│  Google Sheets  (원본 · single source)   │
  (입력·붙여넣기) │  ⭐️분당부부_MASTER   ★아파트 투자_Master  │
                 └───────────────┬─────────────────────────┘
                                 │ 읽기 (서비스 계정)
            ┌────────────────────┼────────────────────┐
            ▼                                         ▼
  ┌──────────────────────┐                ┌──────────────────────┐
  │ bundang-market-report│                │   asset-dashboard    │
  │  (텔레그램·메일 리포트)│                │  (웹 대시보드)         │
  │  + 시트에 되쓰기       │───체크포인트──▶ │  GitHub Pages        │
  └──────────────────────┘                └──────────────────────┘
```

**철칙: 시트가 원본, 웹은 미러.** 값을 고치려면 시트를 고친다.
웹에서 계산해 시트와 두 벌이 되면 언젠가 반드시 어긋난다.

예외는 하나 — 화면의 '지금 순자산'은 실시간 평가액으로 맞춘다(시트는 월 단위 기록).
그 차이는 표 아래 주석으로 항상 밝힌다.

## 2. 두 레포가 하는 일

| | `bundang-market-report` | `asset-dashboard` |
|---|---|---|
| 역할 | 리포트 생성·발송, 시트 **쓰기** | 웹 렌더링, 시트 **읽기** |
| 산출 | 텔레그램·이메일, 시트 체크포인트 | `docs/index.html` + 암호화 데이터 |
| 배포 | GitHub Actions | GitHub Pages |
| 쓰기 권한 | 있음 (시트 갱신) | 없음 (읽기 전용이 원칙) |

## 3. 갱신 체계

| 잡 | 레포 | 주기 | 하는 일 |
|---|---|---|---|
| `daily_report.yml` | bundang | 06/07시·16:30·22:25·토 08시 | 리포트 발송 + 체크포인트 기록 |
| `officetel.yml` | bundang | 4시간 | 붙여넣기 큐 소화 + 실거래 시세 + 별점 |
| `live.yml` | dashboard | 30분(+크론 폴백) | 시세·뉴스·시트 블록 → `live-data` 브랜치 |
| `update.yml` | dashboard | 일 07:30 · 금 08:30 · 말일 | KB시세·배당·이력 → `main` |

**GitHub schedule은 공개 레포에서 지킨다는 보장이 없다.** 이 계정 실측 중앙값 100분
(기대의 27%). 정시가 필요하면 Apps Script가 `workflow_dispatch`로 깨우고, 크론은
그 트리거가 죽었을 때의 폴백으로만 남긴다.

### 신선도 등급 — '얼마나 어긋나도 되는가'로 나눈다

| 등급 | 목표 지연 | 무엇 | 어디서 |
|---|---|---|---|
| **T1** | **≤10분** | 주식·환율·계좌 평가액 | `live.yml` 10분 + `quotes.py`가 가격 직접 조회 |
| **T2** | ≤3시간 | 가계부·FIRE 목표·시뮬레이션·부채·월별자산 | `live.yml` TTL 180 |
| **T3** | ≤4시간 | 오피스텔 실거래 | `live.yml` TTL 240 · `officetel.yml` 4시간 |
| **T4** | ≤12시간 | 도시 비교·연간 리뷰 | `live.yml` TTL 720 |
| **주간** | 주 1회(금) | KB 부동산 시세 | `update.yml` 금 08:30 |

**무료로 실시간 시세를 주는 곳은 없다.** 정직하게 적어 둔다.
미국 ETF는 Yahoo가 사실상 실시간이라 10분 주기면 화면도 10분 이내지만,
국내 ETF(.KS)는 Yahoo도 15~20분 지연이라 25~30분이 된다.
종전에는 시트 GOOGLEFINANCE(15~20분) + 시트 재계산 시점 불확실 + 30분 주기라
최대 ~50분이었다. `quotes.py`가 수량×현재가를 직접 곱해 그 사슬을 끊었다.
시세를 못 받으면 **시트 값을 그대로 둔다** — 0으로 떨어뜨리면 순자산이 통째로 흔들린다.

주기를 바꾸면 `tools/trigger_live.gs`의 `setUpTrigger`를 **반드시 다시 실행**해야 한다.
Apps Script 시간 트리거는 기존 것을 지우고 새로 걸어야 주기가 바뀐다.

### TTL — 블록마다 수명이 다르다
`update_live.py`의 `TTL_MIN`. 0이면 매번, 그 외는 그만큼 지나야 다시 읽는다.
값이 아니라 **읽기**를 아끼는 것이라 페이로드는 늘 완전하다.
읽기에 실패해도 직전 발행본을 싣는다 — 아침 스냅샷으로 후퇴시키지 않는다.

파싱 코드를 고치면 `_code_fingerprint()`가 바뀌어 TTL이 남아 있어도 다시 읽는다.
**새 리더 모듈을 만들면 지문 대상에 넣을 것** (안 넣으면 최대 TTL 동안 옛 결과가 나간다).

## 4. 품질 기준 (이 선 아래로 내려가지 않는다)

### 성능 예산
- 첫 화면 이미지 **1회 다운로드 ≤ 650KB** (현재: 큰화면 603 / 노트북 310 / 폰 155KB)
- 본문 폰트 **≤ 480KB** (현재 441KB — Pretendard 가변 자체 서브셋)
- 외부 요청 0개 — CSS·JS 전부 인라인, 폰트·파비콘도 자체 호스팅
- 데이터는 `raw.githubusercontent.com` 직접 수신 (CORS 허용 + 5분 캐시)

### 디자인
- **색은 반드시 토큰으로.** `--surface-*` `--ink-*` `--pos/--neg/--accent/--warn`
- 3단 테마: 바탕 `:root`(라이트) → `@media (prefers-color-scheme: dark)` →
  `[data-theme="dark"]`. 어느 하나에만 정의된 색을 만들지 않는다.
- 숫자는 `--mono` + `font-variant-numeric: tabular-nums`. 자리가 흔들리면 계기판이 아니다.
- 넓은 표는 자기 컨테이너에서 `overflow-x:auto`. 페이지 본문은 가로 스크롤 금지.

### 접근성
- 값이 바뀌는 곳에 `aria-live` (갱신 시각=polite, 오류=assertive)
- 탭은 `role=tablist/tab/tabpanel` + **전환 로직에서 `aria-selected`를 같이 바꾼다**
- 장식 레이어는 `aria-hidden`, 아이콘 버튼은 `aria-label`
- `prefers-reduced-motion` 존중, `:focus-visible` 유지

### 정보 설계
- **모든 숫자는 출처와 시점을 밝힌다.** "언제 값인지" 없는 숫자는 넣지 않는다.
- 가정이 걸린 숫자는 가정을 함께 쓴다 (예: 경기순환 반영 여부, 생활비 절감률).
- 신선도가 중요한 값은 경과 시간을 병기하고, 오래되면 흐리게 눕힌다.

## 5. 사고 기록 — 같은 실수를 두 번 하지 않기 위해

### 시트
- **`batch_clear` 후 `update`가 실패해 전 행이 날아갔다.** 지우기 전에 쓰지 마라.
  쓰기가 검증된 뒤에 지운다. 되돌릴 데가 없다.
- **여러 행을 쓰면서 매번 `len(grid)`를 다시 계산해 16건이 한 행에 겹쳐 쓰였다.**
  커서를 하나 두고 증가시킨다.
- **읽기 범위가 세 번 잘렸다** (BD→BH→BI). 화면에서는 "값이 없는 것"처럼 보여
  원인을 찾기 어렵다. 범위는 시트 끝까지 넉넉히 잡는다.
- **`gspread`의 `Worksheet.batch_update`는 넘긴 dict의 `range`를 제자리에서 바꾼다.**
  호출 뒤 같은 리스트를 다시 걸러 쓰면 엉뚱한 시트로 간다. 시트별 목록을 미리 나눈다.
- **`=HYPERLINK()`는 기본 조회로 표시문구만 온다.** 그대로 href에 넣어 404가 났다.
  `value_render_option="FORMULA"`로 URL을 뽑는다.
- **`TEXTJOIN`은 `IF`의 배열을 스스로 펼치지 않는다.** `ARRAYFORMULA`로 감싼다.
  안 그러면 첫 항목만 나오는데, 틀린 걸 알아채기 어렵다.
- **되쓰기 순환**: ★주식계좌↔★월별자산이 서로 쓰면 오기록이 영구히 굳는다.

### 워크플로
- **조용히 안 도는 것이 가장 나쁘다.** 2026-08-26 모닝 리포트가 통째로 유실됐는데
  코드는 멀쩡했고, 시트가 안 바뀌니 대시보드는 어제 운세를 아무 표시 없이 보여줬다.
  화면에는 **밀렸다는 사실이 드러나게** 한다. 재시도는 **따라잡기 크론을 따로 깔지 말고**
  크론을 목표 시각에 걸어 지각분이 즉시 실행되게 한다(아래 과금 항목) — 멱등 가드가 있으면
  지각이 곧 따라잡기다. 크론을 더할 때는 **잡의 `if:` 게이트에도 반드시 추가한다.**
  빠뜨리면 워크플로만 깨우고 전 잡이 skip되는 '죽은 크론'이 되어, 역시 조용히 안 돈다.
- **러너에서 기다린 시간은 전액 청구된다 — GitHub은 일한 시간이 아니라 러너를 붙잡은
  벽시계 시간에 과금한다.** 정시 발송을 맞추려고 크론을 목표보다 60분 앞에 걸고 그 60분을
  `time.sleep()`으로 메웠다. 비공개 레포라 그 수면이 전부 청구서가 됐다 — 월 추정 5,476분 중
  4,680분(85%)이 잠든 시간, 무료 한도 2,000분의 2.7배. 계정 지출 한도가 걸리자 **그 레포뿐 아니라
  계정의 모든 비공개 레포 Actions가 함께 죽었다**(2026-08, 다른 대시보드 `heartbeat.yml` 동반 실패).
  기다리지 말고 **크론을 목표 시각 자체로 옮긴다.** 목표보다 이르게 깨어난 스케줄 실행은
  자지 말고 종료한다(사람이 누른 `workflow_dispatch`는 예외 — 즉시 실행).
- **`timeout-minutes`가 없으면 GitHub 기본은 360분이다.** 멈춘 잡 하나가 비공개 한도의 18%를
  혼자 태운다. 실소요가 2~3분이면 15분쯤으로 못 박는다. 초과는 사고이니 실패로 알리게 둔다.
- **DST 듀얼 크론의 판정은 '남은 시간'이 아니라 '어느 크론이 깨웠나'로 한다.**
  크론을 목표 시각에 걸면 기존의 '목표까지 60분 남은 쪽이 진짜' 창이 성립하지 않고,
  이 계정 특유의 60~120분 지각까지 겹치면 시간만으로는 맞는 크론과 반대 계절 크론을
  가를 수 없다. `github.event.schedule` 문자열은 지각과 무관하게 결정적이다.
- **워크플로 수준 `concurrency` 그룹에 템플릿 식을 쓰면 트리거마다 값이 갈린다.**
  직렬화가 안 걸려 같은 리포트가 두 번 나갔다. 잡마다 **리터럴 이름**으로 선언한다.
- **로컬에서 `run_ci`를 돌려 옛 평문이 `.enc`를 덮어써 history 156행이 날아갔다.**
  `.enc`가 언제나 원본, 평문은 사본. 실행 전 `git status`.
- **`httpx`는 INFO에서 요청 URL을 통째로 찍는다.** 공공데이터 `serviceKey`가
  쿼리스트링에 있어 Actions 로그에 키가 남는다. `WARNING`으로 낮춘다.
- **`actions/checkout`은 실행 생성 시점의 SHA를 받는다.** 그 뒤 커밋은 안 보인다.

### 프런트엔드
- **경과 '시간'으로 일수를 세면 오늘 것이 '어제'로 표시된다.** 달력 날짜로 센다.
- **히어로를 전 화면 2000px로 줄였다가 큰 화면에서 뭉갰다.** 하나로 줄이지 말고
  화면 폭으로 나눈다.
- **CSS 클래스 이름 충돌** — 기존 `td .chip`(8×8 점) 위에 새 `.chip`을 얹어 찌그러졌다.
  새 이름을 붙인다.
- **탭 목록을 배열로 하드코딩하면 탭을 늘릴 때 잊는다.** 버튼에서 직접 읽는다.
- 토큰이 아닌 색을 쓰면 다크에서 반드시 깨진다.
- **레이어를 돌려 쓸 때는 앞 내용을 반드시 지워라.** 표지 순환이 레이어 2개를 번갈아
  쓰는데 `paint()`가 배경만 바꾸고 `<video>`를 안 걷어냈다. 영상 두 컷이 한 번씩
  지나가자 두 레이어가 모두 영상을 품어, 그 뒤 모든 사진 위에서 영상이 계속 돌았다.
- **주기를 바꾸면 전환·애니메이션도 같이 바꿔라.** 머무는 시간만 9s→5s로 줄이면
  1.6s 전환이 슬롯의 3분의 1을 먹고, 24s 켄번스는 5분의 1만 재생돼 정지처럼 보인다.
- **구문 검사는 스코프 오류를 못 잡는다.** `freshnessTip(live)`를 넣었는데 그 스코프에
  `live`가 없어(인라인 인자였다) ReferenceError가 났고, `node --check`는 통과했다.
  렌더러를 고쳤으면 스텁 데이터를 넣고 **실제로 태워 봐야** 한다.
- **한 위젯의 실패가 화면 전체를 막지 않게 하라.** 위 오류로 render()가 던지자
  바로 뒤의 `dismissIntro()`에 닿지 못해 인트로에 영원히 갇혔다. 화면을 여는 일은
  `finally`에 둔다.

### 모델링
- **분자·분모에 같은 보정을 넣으면 그 효과가 상쇄된다.** 필요자금 조정계수에서
  절감이 사라져, 소진은 늦어지는데 목표액은 올라가는 모순이 났다.
  비율의 분모는 언제나 '아무 보정도 없는 기준선'이어야 한다.
- **필요자금(현재가치)은 둔감하고 소진 나이는 민감하다.** 뒤쪽 20년은 명목 98억이어도
  현재가치로는 2억(8%)이다. 리스크는 소진 나이로 판단한다.
- 기대수익률에는 이미 역사적 폭락이 들어 있다. 폭락을 따로 더하면 이중계산이다.

### 뉴스 — 검색이 아니라 매체를 고정한다
구글 뉴스 검색 RSS는 무엇이 들어올지 통제할 수 없다. 스팸을 블랙리스트로 막는 건
끝없는 두더지잡기다(카지노 기사가 배당ETF 태그로 3위에 오른 적이 있다).
**신뢰 매체 6곳의 섹션 RSS만 받고**, 그 안에서 관심 주제 키워드로 추린다.
종합 피드는 쓰지 않는다 — 스포츠·연예가 섞인다.
키워드는 좁게 잡는다: '달러'는 '탄자니아 차관'을, '주택'은 정치 기사를 끌고 온다.
개수를 못 채우면 **기간만** 넓힌다. 매체·주제 기준은 절대 풀지 않는다.

### 수집 윤리
- **네이버·KB·직방·호갱노노·아실·다방은 전부 robots.txt로 자동수집을 막는다**
  (2026-08 재확인). 긁지 않는다. 사람이 복사해 온 것을 받아쓰기만 한다.
- 공공데이터(국토부 실거래, 청약홈)는 공개 API가 있다. 이쪽만 자동화한다.
- data.go.kr은 **서비스별로 활용신청이 따로**다. 같은 키인데 한쪽만 될 수 있다.

## 5-1. 소스 구조 — docs/index.html은 산출물이다

`docs/index.html`을 직접 고치지 마라. `build.py`가 `src/`에서 굽는다.
직접 고치면 다음 빌드에 조용히 덮인다 (`dashboard-ship` 점검이 이걸 잡는다).

```
src/index.template.html  →  @@STYLES@@ · @@APP@@ 자리에 끼워 넣는다
src/styles.css
src/js/00-util · 10-charts · 20-hero-fire · 30-assets
       40-spending · 50-tabs · 90-runtime      ← 이름 순 = 실행 순
```

`python build.py` 로 굽고, `python build.py --check` 로 어긋났는지만 볼 수 있다.
모듈을 더할 때 번호를 붙이는 이유는 최상위 `const`가 호이스팅되지 않기 때문이다 —
순서가 바뀌면 에러 없이 조용히 깨진다.

## 6. 자주 하는 작업

### 시트에 열을 더할 때
1. 시트 `col_count` 확인 → 모자라면 `add_cols`
2. 헤더 라벨을 쓴다 (코드는 **열 위치가 아니라 라벨**로 찾는다)
3. 리더의 읽기 범위·패딩 폭이 새 열을 덮는지 확인
4. 사람이 넣는 칸이면 노란 배경 (수식 칸은 칠하지 않는다 — 덮어쓰게 된다)

### 대시보드에 블록을 더할 때
1. `scripts/sheets_*.py`에 리더 (실패 시 `{}` 반환, 탭 숨김)
2. `update_data.py`(일별)와 `update_live.py`(30분) 양쪽에 등록
3. `TTL_MIN`에 수명 추가 → **`live` payload에도 같은 키로 실을 것**
   (자기점검이 막아 주지만, 막히면 실행이 죽는다)
4. `index.html`의 `applyLive` 키 목록에 추가
5. 렌더러 작성 → 켜짐/꺼짐 양쪽 상태를 실제로 렌더해 확인

### 배포 전 점검
```bash
# JS 구문
python -c "import re;s=open('docs/index.html',encoding='utf-8').read();open('/tmp/c.js','w',encoding='utf-8').write(''.join(m.group(1) for m in re.finditer(r'<script(?![^>]*src=)[^>]*>(.*?)</script>',s,re.S)))" && node --check /tmp/c.js
# 평문 금융데이터가 추적되지 않는지
git status --short | grep -E '\.(json|csv|yaml)$' && echo "⚠️ 평문 확인 필요"
```

## 7. 무료 범위 안에서

전부 무료 티어다. 넘지 않도록 지킨다.
- GitHub Actions — **공개 레포만 무제한. 비공개는 2,000분/월이고, 한도를 넘기면
  계정의 모든 비공개 레포가 함께 멈춘다.** `asset-dashboard`는 공개라 자유롭지만
  `bundang-market-report`는 비공개(`data/net_worth_history.jsonl`이 평문 순자산)라
  분을 센다 — 현재 추정 736분/월. 러너에서 자지 않는 것이 이 숫자를 지키는 핵심이다.
  회귀는 `bundang-market-report/tests/test_actions_budget.py`가 막는다.
- GitHub Pages — 100GB/월, 소프트 한도
- Google Sheets API — 분당 60 읽기/사용자. TTL 캐시가 이걸 지킨다
- 공공데이터포털 — 개발계정 일 10,000건. 실거래는 하루 144콜(1.5%)
- Gemini API — 무료 티어. 운세는 캐시로 중복 호출을 막는다
