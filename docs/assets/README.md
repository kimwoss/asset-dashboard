# 인트로 히어로 이미지

진입 인트로(시네마틱 랜딩)의 배경 사진입니다.

- **파일명**: `hero.jpg` (이 폴더에 저장 → `docs/assets/hero.jpg`)
- 권장: 가로가 긴 풍경 사진, 최소 2000px 폭, JPEG(가능하면 500KB~1.5MB로 최적화)
- 현재 기획 사진: **2022년 포르투 도오루(Douro) 강변**

`hero.jpg`가 없으면 인트로는 포르투 톤의 그라디언트로 우아하게 대체 표시됩니다(깨지지 않음).
사진을 바꾸고 싶으면 같은 이름(`hero.jpg`)으로 교체 후 커밋하면 됩니다.

초점 위치는 `docs/index.html`의 `#intro .bg`에서 `center 42%`로 잡혀 있습니다(지붕·수면 라인 강조).
